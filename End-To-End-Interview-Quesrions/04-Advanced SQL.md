1. Find the second-highest salary in every department.
2. Find customers whose latest order was unsuccessful.
1. Remove duplicate records while retaining the latest record.
1. Calculate a seven-day rolling average.
1. Find consecutive days on which a customer placed an order.
1. Find employees earning more than their department average.
1. Compare ROW_NUMBER, RANK and DENSE_RANK.
1. When would you use QUALIFY in Snowflake?
1. Explain the logical execution order of a SQL query.
1. What is the difference between WHERE and HAVING?
1. Why can a LEFT JOIN behave like an INNER JOIN after filtering?
1. Compare NOT IN, NOT EXISTS and anti-joins, especially with NULL.
1. Explain correlated and non-correlated subqueries.
1. Compare CTEs, temporary tables and views.
1. What is predicate pushdown?
1. How would you tune a query with several large joins?
1. How do join cardinality and duplicate keys affect row counts?
1. How would you identify which join introduced duplicates?
1. Explain SCD Types 1 and 2 using SQL.
1. Write a MERGE statement for an incremental dimension load.
